# Copy to .env.local and fill in. Server-side only (no NEXT_PUBLIC_
# prefix): every fetch to the backend happens in Server Components, never
# in the browser, so this is never bundled to the client.
API_BASE_URL=http://localhost:8000
